# Internal working notes ([NEEDS MARTIJN] flags). Generated by tools/convert.mjs.
# This repo is public, so these must never be committed: they can name colleagues
# and describe unresolved internal process.
content.staff.json

# Local verification scratch
live.json
